apiVersion: v1
kind: Service
metadata:
  name: freenet-org-staging
  namespace: freenetorg
  annotations:
    cloud.google.com/backend-config: '{"default" : "staging-backendconfig"}'
spec:
  selector:
    app: freenet-org-staging
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  type: NodePort